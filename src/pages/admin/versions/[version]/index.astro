---
import AdminLayout from "@/layouts/admin.astro";
import VersionDetail from "@/components/pages/admin/version-detail/index.tsx";

const { version } = Astro.params;

if (!version) {
  return Astro.redirect("/admin/versions");
}
---

<AdminLayout title={`Version ${version} - Admin - EMOBase`}>
  <main class="flex-grow p-8">
    <VersionDetail versionName={version} client:load />
  </main>
</AdminLayout>
